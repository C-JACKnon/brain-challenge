import { ScoreTimePipe } from './score-time.pipe';

describe('ScoreTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreTimePipe();
    expect(pipe).toBeTruthy();
  });
});
